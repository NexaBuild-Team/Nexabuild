generator client {
  provider = "prisma-client"
  output   = "../../src/generated/prisma"
}

generator clientJs {
  provider        = "prisma-client-js"
  previewFeatures = []
}

datasource db {
  provider = "postgresql"
}